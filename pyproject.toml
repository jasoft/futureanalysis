[project]
name = "futureanalysis"
version = "0.1.0"
description = "Streamlit report for futures account status analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.3.0",
    "plotly>=6.0.0",
    "streamlit>=1.44.0",
]

[tool.uv]
package = false
